const obj = {
  place: "A",
  city: "Barshi",
  pin: 413401,
};
//Convert JSON to string
const str = JSON.stringify(obj);
console.log(str);

// Convert String to JSON

const objstr = JSON.parse(str);
console.log(objstr);
console.log(objstr);
